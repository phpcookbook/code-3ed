<?php
assert($response1 === "", $response1);
assert($response2 === "", $response2);
assert($response3 === "Submitted email address is invalid.", $response3);
assert($response4 === "Submitted email address is invalid.", $response4);

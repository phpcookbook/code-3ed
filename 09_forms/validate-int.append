<?php
assert($response1 === "", $response1);
assert($response2 === "", $response2);
assert($response3 === "", $response3);
assert($response4 === "Submitted age is invalid.", $response4);
assert($response5 === "Submitted age is invalid.", $response5);
